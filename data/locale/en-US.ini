FilterName="Background Removal (RMBG / RVM)"
ModelPath="Model file (RMBG-1.4 or RVM ONNX)"
RVMOptions="RVM settings (used only with an RVM model)"
RVMMaxSize="Maximum input long edge (pixels)"
RVMDownsample="Downsample ratio"
RVMForeground="RVM foreground colors (experimental; matched video only)"
QualityPreset="Apply 1080p30 quality settings"
RVMAuto="Automatic (480-pixel internal long edge)"
Device="Inference device"
DeviceAuto="Automatic (CUDA, then CPU)"
DeviceCPU="CPU"
DeviceCUDA="CUDA GPU (requires compatible runtime and libraries)"
Threads="CPU threads"
MaxFPS="Maximum mask updates per second"
ImmediateReadback="Low latency readback (disable if rendering stalls)"
MatchVideo="Match video to mask (buffers video; disables extra smoothing)"
Smoothing="Temporal smoothing (live video mode only)"
Threshold="Mask threshold"
Softness="Edge softness"
StaleTimeout="Discard mask older than (milliseconds)"
Preview="Show mask instead of video"
Status="Status"
Refresh="Refresh status / retry model"
UsageNote="SDR sources only. Loading, missing model, errors, and stale masks show the original source (background visible). Matching video retains source detail on the GPU and repeats the last complete pair until a new one is ready. Audio is not delayed. Processing speed depends on your hardware."
