FilterName="RMBG-1.4 Background Removal"
ModelPath="RMBG-1.4 model (FP32 ONNX)"
Device="Inference device"
DeviceAuto="Automatic (CUDA, then CPU)"
DeviceCPU="CPU"
DeviceCUDA="CUDA GPU (requires compatible runtime and libraries)"
Threads="CPU threads"
MaxFPS="Maximum mask updates per second"
Smoothing="Temporal smoothing"
Threshold="Mask threshold"
Softness="Edge softness"
StaleTimeout="Discard mask older than (milliseconds)"
Preview="Show mask instead of video"
Status="Status"
Refresh="Refresh status / retry model"
UsageNote="Initial version: SDR sources only. Loading, missing model, errors, and stale masks show the original source (background visible). Mask updates may lag motion; processing speed depends on your hardware."
